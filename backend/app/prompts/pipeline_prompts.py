SYSTEM_PROMPT = """Ты — AI-агент "Бизнес-аналитик-Корпоративный архитектор" (AI-ba-agent).

Твоя задача: извлекать требования к разработке ПО из неструктурированных текстов (протоколы встреч, письма, интервью с заказчиком).

## Методологическая база
- **Volere ReqSpec v16** — структура документа, типы требований, атрибуты snow card, fit criterion, трассируемость
- **BABOK v3** — 8 атрибутов качества: корректность, полнота, непротиворечивость, осуществимость, необходимость, приоритетность, однозначность, проверяемость
- **EARS** — нормализация в атомарные утверждения: Ubiquitous / Event-driven / State-driven / Unwanted behaviour / Optional feature

## Типы требований (Volere)
BR, UR, FR, NFR-PERF, NFR-USAB, NFR-LOOK, NFR-SECU, NFR-OPER, NFR-MAIN, NFR-CULT, NFR-LEGL, CON, ARCH, ASS, OI, RISK

## Критические правила
1. Не изобретать требований — только из входных данных
2. Каждый [ASSUMPTION] маркировать явно
3. Сохранять трассируемость: поле source = цитата или [SRC-N]
4. Язык входных данных = язык вывода
5. Fit criterion обязателен для всех FR и NFR
6. Неопределённость → OI + уточняющий вопрос
"""

STAGE1_EXTRACT_PROMPT = """## Этап 1: Извлечение кандидатов

Проанализируй текст и извлеки все смысловые фрагменты, которые потенциально несут утверждение о системе/продукте.

Для каждого фрагмента укажи:
- [SRC-N] — номер кандидата
- Цитата (дословно)
- Тег: кандидат | [CONTEXT] | [ASSUMPTION] | [RISK] | [OPEN_ISSUE] | [POLICY]

Входной текст:
{text}

Верни структурированный список кандидатов в формате JSON:
{{
  "candidates": [
    {{"id": "SRC-1", "quote": "...", "tag": "кандидат"}},
    ...
  ],
  "language": "ru"
}}
"""

STAGE2_CLASSIFY_PROMPT = """## Этап 2: Классификация по типам Volere

Для каждого кандидата из списка определи тип требования по Volere ReqSpec v16.

Типы: BR, UR, FR, NFR-PERF, NFR-USAB, NFR-LOOK, NFR-SECU, NFR-OPER, NFR-MAIN, NFR-CULT, NFR-LEGL, CON, ARCH, ASS, OI, RISK

Кандидаты:
{candidates_json}

Верни JSON:
{{
  "classified": [
    {{"id": "SRC-1", "type": "FR", "explanation": "..."}}
  ]
}}
"""

STAGE3_NORMALIZE_PROMPT = """## Этап 3: Нормализация в атомарные требования (EARS/Volere)

Для каждого классифицированного требования:
1. Применить подходящий EARS-шаблон:
   - Ubiquitous: "Система должна <действие>"
   - Event-driven: "Когда <событие>, система должна <действие>"
   - State-driven: "Пока <состояние>, система должна <действие>"
   - Unwanted behaviour: "Если <нежелательное условие>, система должна <защитное действие>"
   - Optional feature: "Если продукт включает <функцию>, то система должна <действие>"

2. Заполнить snow card Volere для каждого требования

Классифицированные кандидаты:
{classified_json}

Оригинальный текст для трассируемости:
{original_text}

Верни JSON-массив snow cards:
{{
  "requirements": [
    {{
      "req_id": "FR-001",
      "req_type": "FR",
      "ears_pattern": "Event-driven",
      "description": "Когда..., система должна...",
      "rationale": "...",
      "source": "[SRC-1] цитата",
      "originator": "заказчик / tbd",
      "fit_criterion": "...",
      "priority": "high|medium|low|tbd",
      "conflicts": [],
      "status": "extracted|clarification_needed|assumed",
      "notes": ""
    }}
  ]
}}
"""

STAGE4_QUALITY_PROMPT = """## Этап 4: Проверка качества по BABOK v3

Проверь набор требований по 8 атрибутам качества BABOK v3:
1. Корректность — точно отражает намерение стейкхолдера?
2. Полнота — все сценарии описаны, нет «дыр»?
3. Непротиворечивость — нет конфликтов?
4. Осуществимость — реалистично?
5. Необходимость — не «хотелка»?
6. Приоритетность — приоритет назначен?
7. Однозначность — одна интерпретация?
8. Проверяемость — есть fit_criterion?

Требования:
{requirements_json}

Верни JSON:
{{
  "quality_issues": [
    {{"req_id": "FR-001", "attribute": "Проверяемость", "issue": "отсутствует fit_criterion", "severity": "high"}}
  ],
  "conflicts": [
    {{"req_ids": ["FR-001", "FR-003"], "description": "..."}}
  ],
  "gaps": [
    "Отсутствуют требования по безопасности (NFR-SECU)",
    ...
  ]
}}
"""

STAGE5_QUESTIONS_PROMPT = """## Этап 5: Уточняющие вопросы

На основе анализа сформируй список уточняющих вопросов к пользователю.

Правила:
- Конкретный вопрос — конкретный ответ меняет требование
- Ссылаться на req_id или SRC-N
- Приоритеты: [BLOCKER] | [HIGH] | [MEDIUM] | [LOW]

Требования с проблемами:
{requirements_json}

Проблемы качества:
{quality_issues_json}

Верни JSON:
{{
  "questions": [
    {{
      "id": "Q-001",
      "priority": "BLOCKER",
      "ref": "FR-001",
      "text": "...",
      "why": "...",
      "options": ["A) ...", "B) ..."]
    }}
  ]
}}
"""

STAGE6_JSON_PROMPT = """## Этап 6: Финальный JSON по схеме Volere

Сформируй финальный JSON-документ по схеме Volere Requirements Specification.

Данные:
- Требования: {requirements_json}
- Проблемы качества: {quality_issues_json}
- Название проекта: {project_name}

Верни валидный JSON строго соответствующий структуре Volere ReqSpec v16.
Поля без данных заполни "tbd" или пустыми массивами [].
"""

CLARIFICATION_PROMPT = """Пользователь ответил на уточняющие вопросы:

{answers}

На основе ответов обнови следующие требования (укажи только изменённые):

Текущие требования:
{requirements_json}

Верни обновлённый JSON-массив только изменённых требований (тот же формат snow card).
"""
